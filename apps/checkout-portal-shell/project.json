{
  "name": "checkout-portal-shell",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/checkout-portal-shell/src",
  "projectType": "application",
  "tags": ["type:shell"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "rspack serve --mode=development",
        "cwd": "apps/checkout-portal-shell"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rspack build --mode=production",
        "cwd": "apps/checkout-portal-shell"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/checkout-portal-shell"],
      "options": {
        "jestConfig": "apps/checkout-portal-shell/jest.config.cts"
      }
    }
  }
}
