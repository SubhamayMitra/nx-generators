{
  "$schema": "https://json-schema.org/schema",
  "$id": "Feature",
  "title": "Feature",
  "description": "Scaffold a new feature/module inside an existing MFE.",
  "type": "object",
  "properties": {
    "mfeName": {
      "type": "string",
      "description": "The target MFE's project name, e.g. storefront-mfe-search (must already exist).",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "Which MFE is this feature for (e.g. storefront-mfe-search)?"
    },
    "name": {
      "type": "string",
      "description": "Feature name, e.g. saved-searches.",
      "$default": { "$source": "argv", "index": 1 },
      "x-prompt": "What name would you like to use for this feature (e.g. saved-searches)?"
    }
  },
  "required": ["mfeName", "name"]
}
